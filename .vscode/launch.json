{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "gdb",
            "request":"launch",
            "target": "./bin/test",
            "preLaunchTask": "Compile",
            "cwd" : "${workspaceRoot}"
        },

        {
            "name": "Debug (lldb)",
            "type": "lldb-mi",
            "request":"launch",
            "target": "./bin/test",
            "preLaunchTask": "Compile",
            "cwd" : "${workspaceRoot}"
        }
    ]
}